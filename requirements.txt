# Dashboard runtime only.
#
# Streamlit Community Cloud auto-detects this file and has no setting to point
# at another one, so it has to hold the slim list. The full development and CI
# dependencies are in requirements-dev.txt - hopsworks pulls in
# confluent-kafka, which needs a C library that is not present on the Streamlit
# runner, and the build fails there.
streamlit>=1.30
plotly>=5.18
pandas>=2.0,<3.0
requests>=2.31
openai>=1.30
